apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "tax-assessment-service.fullname" . }}
  labels:
    {{- include "tax-assessment-service.labels" . | nindent 4 }}
data:
  APP_ENVIRONMENT: {{ .Values.env.APP_ENVIRONMENT | quote }}
